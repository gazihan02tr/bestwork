# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=False

# Flask Secret Key (Otomatik oluşturulacak, gerekirse manuel düzenle)
# Üretmek için: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-will-be-auto-generated

# MongoDB Connection
# Yerel kurulum: mongodb://localhost:27017/bestwork
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/bestwork
MONGO_URI=mongodb://localhost:27017/bestwork

# TCKN Encryption Key (Otomatik oluşturulacak, gerekirse manuel düzenle)
# Üretmek için: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
TCKN_SECRET_KEY=your-tckn-secret-key-here-will-be-auto-generated

# Demo User Credentials (Test amaçlı)
DEMO_USER_ID=000954
DEMO_USER_PASS=12345

# Redis Cache (Opsiyonel - Performance için önerilir)
# Yerel kurulum: redis://localhost:6379/0
# Redis Cloud: redis://username:password@host:port
REDIS_URL=redis://localhost:6379/0

# Rate Limiting Storage (Opsiyonel)
RATELIMIT_STORAGE_URL=redis://localhost:6379/1

# Database Connection Pool Settings (Opsiyonel)
MONGO_MAX_POOL_SIZE=50
MONGO_MIN_POOL_SIZE=10

# Logging Level
LOG_LEVEL=INFO

# Application Port (Opsiyonel)
FLASK_PORT=5000

# Application Host (Opsiyonel)
FLASK_HOST=127.0.0.1

